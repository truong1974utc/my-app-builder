export enum EStatusProduct {
    ACTIVE = "ACTIVE",
    INACTIVE = "INACTIVE",
}

export enum EStatusPage {
    PUBLISHED = "PUBLISHED",
    DRAFT = "DRAFT",
}

export enum EStatusUser {
    ACTIVE = "ACTIVE",
    INACTIVE = "INACTIVE",
}

